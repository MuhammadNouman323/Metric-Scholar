<div class="department-icon inline-flex items-center justify-center {{ $wrapperClass }} {{ $class ?? 'w-10 h-10' }}" data-department="{{ $department }}">
    <div class="w-full h-full flex items-center justify-center text-[#0e48c1]">
        {!! $svg !!}
    </div>
</div>
